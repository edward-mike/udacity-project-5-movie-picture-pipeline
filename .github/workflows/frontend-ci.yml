# GitHub Actions for Frontend Continuous Integration

name: 👨‍💻🔄 Frontend Continuous Integration 

on:
  workflow_dispatch:  # Allows manual triggering of the workflow
  pull_request:
    branches:
      - master
    paths:
      - 'starter/frontend/**'  # Trigger only if files in the frontend directory change

jobs:
  Setup:
    runs-on: ubuntu-latest

    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4
      
      - name: 🚀 Install Node.js and Dependencies
        uses: actions/setup-node@v4
        with:
          node-version: 18.x
          cache: 'npm'
          cache-dependency-path: starter/frontend/package-lock.json

      - name: 📦 Install Project Dependencies
        working-directory: starter/frontend
        run: npm ci  # Clean install of project dependencies

  Linting:
    runs-on: ubuntu-latest
    needs: Setup  # Ensures that the setup job completes first

    steps:
      - name: 🔍 Run Lint
        run: npm run lint --prefix starter/frontend  # Run linting on the frontend directory

  UnitTests:
    runs-on: ubuntu-latest
    needs: Setup  # Ensures that the setup job completes first

    steps:
      - name: 🧪 Run Tests
        run: npm test --prefix starter/frontend  # Run unit tests in the frontend directory

  Build:
    runs-on: ubuntu-latest
    needs: [Linting, UnitTests]  # Runs only after lint and test jobs succeed

    steps:
      - name: 📂 Checkout Repository
        uses: actions/checkout@v4

      - name: 🐳 Build and Cache Docker Image
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache  # Cache path for Docker layers
          key: ${{ runner.os }}-docker-${{ github.sha }}  # Unique cache key
          restore-keys: |
            ${{ runner.os }}-docker-  # Fallback to restore previous caches

      - name: 🐳 Build and Run Docker Image
        run: |
          cd starter/frontend
          docker build --build-arg=REACT_APP_MOVIE_API_URL=http://localhost:5000 --tag=mp-frontend:latest .
          docker run --name mp-frontend -p 3000:3000 -d mp-frontend 
